pipeline {

    agent none

    environment {
        GIT_URL       = "https://github.com/Vandu0409/jenkins.git"
        GIT_BRANCH    = "k8s-jenkins"

        IMAGE_REPO    = "vandu0409/javaapp"
        IMAGE_TAG     = "${BUILD_NUMBER}"
        FULL_IMAGE    = "docker.io/${IMAGE_REPO}:${IMAGE_TAG}"

        K8S_NAMESPACE = "javaapp"
        RELEASE_NAME  = "javaapp"
    }

    stages {

        stage("Build + Image + Deploy (Single Pod)") {

            agent {
                kubernetes {
                    yaml """
apiVersion: v1
kind: Pod
spec:

  containers:

  - name: maven
    image: maven:3.9.6-eclipse-temurin-21
    command: ["cat"]
    tty: true
    volumeMounts:
    - name: workspace
      mountPath: /home/jenkins/agent

  - name: kaniko
    image: gcr.io/kaniko-project/executor:v1.9.1-debug
    command: ["/busybox/sleep"]
    args: ["999999"]
    tty: true
    volumeMounts:
    - name: docker-config
      mountPath: /kaniko/.docker/
    - name: workspace
      mountPath: /home/jenkins/agent

  - name: helm
    image: alpine/helm:3.11.2
    command: ["cat"]
    tty: true
    volumeMounts:
    - name: workspace
      mountPath: /home/jenkins/agent

  - name: jnlp
    image: jenkins/inbound-agent:latest
    args: ["-workDir=/home/jenkins/agent"]
    tty: true
    volumeMounts:
    - name: workspace
      mountPath: /home/jenkins/agent

  volumes:
  - name: workspace
    emptyDir: {}
  - name: docker-config
    secret:
      secretName: regcred
      items:
      - key: .dockerconfigjson
        path: config.json
"""
                }
            }

            stages {
                stage("Clone Repo") {
                    steps {
                        container("maven") {
                            sh """
                                git clone --branch ${GIT_BRANCH} ${GIT_URL} src
                            """
                        }
                    }
                }

                stage("Build JAR") {
                    steps {
                        container("maven") {
                            sh """
                                cd src/javaapp-standalone
                                mvn -Dmaven.test.skip=true clean package
                            """
                        }
                    }
                }

                stage("Build Docker Image (Kaniko)") {
                    steps {
                        container("kaniko") {
                            sh """
                                /kaniko/executor \
                                  --dockerfile=src/javaapp-standalone/Dockerfile \
                                  --context=/home/jenkins/agent/workspace/${JOB_NAME}/src/javaapp-standalone \
                                  --destination=${FULL_IMAGE}
                            """
                        }
                    }
                }

                stage("Deploy to Kubernetes") {
                    steps {
                        container("helm") {
                            sh """
                                helm upgrade --install ${RELEASE_NAME} src/javaapp-standalone/chart/my-java-app \
                                  --namespace ${K8S_NAMESPACE} \
                                  --set image.repository=docker.io/${IMAGE_REPO} \
                                  --set image.tag=${IMAGE_TAG}
                            """
                        }
                    }
                }
            }
        }
    }
}
